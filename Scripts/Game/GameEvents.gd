extends Node
class_name GameEvents

signal brick_destroyed(points);

signal powerup_collected;

signal powerup_stronger_ball;
signal powerup_more_balls;
signal powerup_extend_paddle;
signal powerup_laser;
signal powerup_catch_ball;
signal powerup_health;
